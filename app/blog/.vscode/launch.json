{
    //** serverSourceRoot : source in the current server's folder
    //** localSourceRoot : Source in the current machine
    "version": "0.2.0",
    "configurations": [
        {
            "name": "PHP Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9001,
            "serverSourceRoot": "/var/www/html",
            "localSourceRoot": "${workspaceRoot}"
        }
    ]
}